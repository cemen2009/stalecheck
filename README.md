# `stalechek`

Scans a project for dependency files, checks each package against the relevant registry (PyPI, npm), 
and reports how outdated things are — with color-coded severity and optional export.

## Project Structure